# Environment variables for HAPI FHIR CDS Hooks Service

# Authentication settings for hackathon deployment protection
# Set to true to enable token-based authentication
AUTH_ENABLED=false

# The access token that must be provided in the X-Access-Token HTTP header
# Only required when AUTH_ENABLED=true
# Example: AUTH_TOKEN=your-secret-token-here
AUTH_TOKEN=
